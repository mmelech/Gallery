{% include 'default/_form.html.twig' with {
    page_title: 'title.gallery_delete'|trans({'%id%': comment.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: '',
} %}
