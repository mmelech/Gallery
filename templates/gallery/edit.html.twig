{% include 'default/_form.html.twig' with {
    page_title: 'title.gallery_edit'|trans({'%id%': gallery.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'gallery_index',
} %}