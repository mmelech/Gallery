{% include 'default/_form.html.twig' with {
    page_title: 'title.category_delete'|trans({'%id%': photo.id|default('')}),
    form: form,
    submit_label: 'action.delete'|trans,
    back_to_list_path: 'photo_index',
} %}
