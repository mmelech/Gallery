{% include 'default/_form.html.twig' with {
    page_title: 'title_useredit'|trans,
    page_title_param: {'%id%': userData.id},
    attr: {
        method: 'PUT',
        action: url('userData_edit', {'id': userData.id}),
    },
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'photo_index',
} %}
